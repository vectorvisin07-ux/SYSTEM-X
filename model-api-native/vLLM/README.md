# System X native branch

This branch is private and idle until ModelFabric authorizes activation. It
loads local Hugging Face safetensors with stock vLLM, `VLLM_PLUGINS` empty and
`trust_remote_code=false`. No GGUF adapter or plugin is installed here.
