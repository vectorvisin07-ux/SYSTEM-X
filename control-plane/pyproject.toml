[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"
[project]
name = "system-x-control-plane"
version = "5.0.0"
requires-python = ">=3.14,<3.15"
[tool.pytest.ini_options]
testpaths = ["tests"]
